import React from 'react';
import Statistics from '../components/Statistics';

const Home=() => {
    return (
        <main>
            <Statistics />
        </main>
    );
};

export default Home;