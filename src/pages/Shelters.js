import React from 'react';
import ShelterList from '../components/ShelterList';

const Shelters =() =>{
    return (
        <main>
            <ShelterList />
        </main>
    );
};

export default Shelters;